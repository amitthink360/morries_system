---
deployment:
      tasks:
        - export DEPLOYPATH=/home/mikerjny/m.mikelmorris.com/new  # Add /<app_folder> if required
        - /bin/cp -r * $DEPLOYPATH                        #copy all recursively to $DEPLOYPATH